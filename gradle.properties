# Plain console makes sure Gradle's progress UI doesn't interfere with input from keyboard
# Meant for tasks like runWasm (where wasmtime reads from stdin via standardInput = System.in)
org.gradle.console=plain
